package view;

public class Ex04StaticMethod {

	public static void main(String[] args) {
		// Muốn gọi hàm non-static ta phải dùng đối tượng của class mới . được
		// Vd Ex04StaticMethod o1 = new Ex04StaticMethod();
		// int n = o1.sum() để gọi hàm SUm non-static
		
		
		// hàm chỉ phụ thuộc tham số tryền vào mà kh phụ thuộc đối tượng đang gọi thì ta dùng non-static

	}

}
