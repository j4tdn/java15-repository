1. Đóng gói Encapsulation
Khái niệm: Tính chất đóng gói được thể hiện qua access modifier. Nếu
+ private ==> class
+ protected/default ==> packet
  ==> Thừa kế - Inheritance
  Khi lớp cha có phương thức, thuộc tính mang access modifier là:
 . protected: class con trong cùng package hoặc ngoài packet đều kế thừa được
 . default : ngoài package không kế thừa được
+ public ==> mọi nơi


2. Đa hình - Polymorphsim
+ Phương thức:
	.Overloading:
	Hai hay nhiều phương thức được gọi là Overloading khi
	-Thuộc 1 class
	-Cùng tên
	-Khác số tham số
	-Khác KDL truyền vào
	.Overriding:
	-Khi override lại thì supper. là gọi hàm trong class cha
	-Hàm thuộc lớp cha, kế thừa, định nghĩa lại trong lớp con
	-Kế thừa cả thuộc tính và phương thức, chỉ đc override lại phương thức
	
+ Đối tượng:
	. Biến thì không override đc nên sẽ theo KDL khi compile
	. Phương thức override đc nên sẽ theo kiểu dữ liệu lúc runtime
	-Tại sao không khai báo trực tiếp cha-cha con-con?
	.Che dấu việc khởi tạo ( liên quan tới factory pattern)
	.cd: API, hidden construction... kiểu như họ cung cấp tài nguyên và tài nguyên đó chứa trong hàm
	mình chỉ cần new là dùng được
	-Lấy KDL của con new cha được không?
	không
	-enum : enumeration: là kiểu dữ liệu mà tại đó tạo ra các đối tượng là hằng số

.Thừa kế:
    -this, super
    Kiểu kế thừa:
    + Class extends class
    + Class extends Abstractclass
    + Class extends Class implements Interface
    ==>Interface: bao gồm abstract methods
    ==> Abstract class: bao gồm abstract và phương thức thường
	==> class: bao gồm abstract và phương thức thường
	
.Trườu tượng
    Hàm trườu tượng là hàm không có thân hàm, chỉ có khai báo

Từ JAVA 8 1 interface có thể chứa hàm có thân hàm

4. Tính trườu tượng:
Khái niệm: Tính chất trườu tượng sẽ được thể hiện qua class, interface, abstract class, method
ví dụ:+ sinh viên bao gồm nhiều thuộc tính... mình chưa biết nó thế nào
	  + interface: có các hàm trườu tượng phải tùy chương trình mà override lại

