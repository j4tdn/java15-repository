/*
	if else switch case
	while, do while, for index, for each
*/

console.log('====== EX03 - Operations ======');

let numbers = [4, 8, 2, 12, 6];

// for-index
for (let i in numbers) {
	console.log(`i --> ${i}`);
}

// for-each
for (let v of numbers) {
	console.log(`v --> ${v}`);
}