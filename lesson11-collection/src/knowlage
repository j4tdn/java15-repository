collection
-->only support object type

-list
-->need to override T's equas method when using contains, remove in List
-->prefer using ArrayList
+ArrayList
	.Array with fixed size
	.size <= length
	.capacity
	
+LinkList(1) vs ArrayList(2)
	.get/set -->(2)
	.add 
	
	